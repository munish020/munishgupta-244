/// to count number of digits in a number ///
#include <stdio.h>

int main()
{
    int num , digits=0;
    
    printf("enter a number :");
    scanf("%d",&num);
    
    while(num!=0)
    {
        num=num/10;
        digits++;
    }
    printf("digits:%d", digits);
}

