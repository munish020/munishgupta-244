/// sum of all natural numbers between 1 to n///
#include <stdio.h>

int main()
{
    int n,i,sum=0;
    printf("enter a positive integer:");
    scanf("%d",&n);
    
    for(i=1;i<=n;i++)
    {
        sum=sum+i;
        
    }
    printf("%d",sum);
    return 0;
}
